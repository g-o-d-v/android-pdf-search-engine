# 示例 PDF

`sample/src/main/assets/test_ocr.pdf` 是项目自行生成的三页合成文档，用于示例应用和公开仓库回归测试：

1. 第 1 页：真实 PDF 文本层；
2. 第 2 页：仅图片的扫描页；
3. 第 3 页：文本层与扫描图片混合页。

内容包含 `音乐`、`乐器`、`扫描页`、`混合搜索`、`169`、`1/16`、`1120mm` 和英文断行等测试词。

该文件不包含书籍、个人资料或第三方文档内容。`sample` 模块不会进入 Library AAR 或 JitPack 依赖。
