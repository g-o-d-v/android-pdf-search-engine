# API 与版本策略

## Alpha 阶段

`0.x` 阶段允许调整公共 API。每次发布应：

- 在 `CHANGELOG.md` 记录用户可见变化；
- 尽量提供弃用过渡；
- 缓存或序列化格式变化时递增对应版本；
- 不复用或移动已经公开发布的 Git 标签。

## 1.0 之后

计划遵循语义化版本：

- PATCH：兼容性修复；
- MINOR：向后兼容的新能力；
- MAJOR：不兼容 API 或行为变化。

## 稳定身份

`PdfSearchResult.resultId` 是逻辑结果身份，列表下标不是稳定身份。缓存文件属于内部实现，第三方不应直接解析。
